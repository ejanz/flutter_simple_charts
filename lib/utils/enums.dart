/// Controls the sort order applied to a chart's dataset before rendering.
enum DatasetOrdering {
  /// Sort data items from smallest to largest value.
  crescent,

  /// Sort data items from largest to smallest value.
  decrescent,
}
